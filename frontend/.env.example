# Backend API base URL — port must match PORT in backend/.env
VITE_API_URL=http://localhost:8000/api

# Set to "true" to use fake/mock data without calling the backend.
# Frontend developers can work without Ollama or the backend running.
VITE_DEV_MODE=false
